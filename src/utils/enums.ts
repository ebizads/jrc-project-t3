// Generator Enums
export enum CommercialPower {
    ON = "ON",
    OFF = "OFF",
}

export enum DegMode {
    GENERATING = "GENERATING",
    MANUAL = "MANUAL",
    AUTO = "AUTO",
}

export enum DegStatus {
    FAILED = "FAILED",
    STANDBY = "STANDBY",
    GENERATING = "GENERATING",
}

export enum RemoteOperation {
    ON = "ON",
    STANDBY = "STANDBY",
    NA = "N/A",
}

export enum LoadOn {
    COMMERCIALPOWER = "COMMERCIAL POWER",
    GENERATOR = "GENERATOR",
}

export enum FuelLevel {
    LOW = "LOW",
    HIGH = "HIGH",
}

// DC 43V Enums
export enum PowerSupply {
    ALARM = "ALARM",
    OPERATING = "OPERATING",
}

export enum BatteryTemp {
    HIGH = "HIGH",
    GOOD = "GOOD",
}

export enum RemoteOperationStatus {
    START = "START",
    STOP = "STOP",
}

export enum ModalStatus {
    LOADING = "LOADING",
    ERROR = "ERROR",
}
